{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 1 production app-command allow-list. Tauri v2 denies every command that is not explicitly named in `permissions`; adding a command requires touching this file, the Rust `#[tauri::command]` definition, and the TS `Commands` map in `@dayseam/ipc-types` in the same PR. Dev-only commands live in a sibling `dev.json` capability that is written by `build.rs` only when the `dev-commands` Cargo feature is enabled, so release bundles carry no dev surface at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-settings-get",
    "allow-settings-update",
    "allow-logs-tail",
    "allow-persons-get-self",
    "allow-persons-update-self",
    "allow-sources-list",
    "allow-sources-add",
    "allow-sources-update",
    "allow-sources-delete",
    "allow-sources-healthcheck",
    "allow-identities-list-for",
    "allow-identities-upsert",
    "allow-identities-delete",
    "allow-local-repos-list",
    "allow-local-repos-set-private",
    "allow-sinks-list",
    "allow-sinks-add",
    "allow-report-generate",
    "allow-report-cancel",
    "allow-report-get",
    "allow-report-save",
    "allow-retention-sweep-now",
    "allow-activity-events-get",
    "allow-shell-open",
    "allow-gitlab-validate-pat",
    "allow-atlassian-validate-credentials",
    "allow-atlassian-sources-add",
    "allow-atlassian-sources-reconnect",
    "dialog:allow-open"
  ]
}
