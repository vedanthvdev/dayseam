// Shared React components for Dayseam.
// Empty in the scaffold phase. Components are added as the desktop app
// grows real layouts in later Phase 1 tasks.

export const UI_PACKAGE_VERSION = "0.0.0" as const;
