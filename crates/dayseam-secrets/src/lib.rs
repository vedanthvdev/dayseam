//! dayseam-secrets: secret storage abstraction and macOS Keychain wrapper.
//! The Secret<T> type and SecretStore trait land in a later Phase 1 task.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles_and_tests_run() {
        assert_eq!(2 + 2, 4);
    }
}
